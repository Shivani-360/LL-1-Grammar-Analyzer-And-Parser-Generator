# LL-1-Grammar-Analyzer-And-Parser-Generator
An LL(1) Grammar Analyzer &amp; Parser built for Compiler Design — detects left recursion, left factoring, and ambiguity, auto-fixes grammars, computes FIRST/FOLLOW sets, builds parsing tables, and visualizes parse trees.
